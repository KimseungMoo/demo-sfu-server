FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends \
    ffmpeg iproute2 iputils-ping curl ca-certificates \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
CMD ["bash"]
